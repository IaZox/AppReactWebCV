import { render, screen } from "@testing-library/react";
import Connect from "../../page/Connect"

describe("Connect", () => {
  it("should render the Buttonconnect", () => {
    render(<Connect />);
  });
});